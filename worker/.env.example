# TEE Randomness Worker Configuration

# Server port
PORT=3000

# Environment (production, development, staging)
NODE_ENV=production

# Payment wallet (receives x402 payments)
PAYMENT_WALLET=YOUR_SOLANA_WALLET_ADDRESS

# GlitchTip Error Monitoring (optional)
# Get DSN from https://glitchtip.com
GLITCHTIP_DSN=

# Whitelisted origins (comma-separated, free access)
# Add your app domains here
WHITELIST=localhost,mysterygift.io,mystery.gift

# API keys for authorized partners (comma-separated, free access)
API_KEYS=your-secret-api-key-1,your-secret-api-key-2

# MRENCLAVE hash (set by TEE environment, leave empty for simulation)
MRENCLAVE=

# ============================================
# x402 PAYMENT CONFIGURATION
# ============================================
# Payment verification is handled by the PayAI facilitator.
# No RPC endpoints needed — the facilitator verifies on-chain payments.

# PayAI facilitator URL
X402_FACILITATOR_URL=https://facilitator.payai.network

# API key for facilitator (optional, for authenticated access)
X402_API_KEY=

# Supported payment networks (comma-separated)
# Options: solana, base
SUPPORTED_NETWORKS=solana

# ============================================
# ARWEAVE IMMUTABLE PROOF CONFIGURATION
# ============================================
# Arweave proof uploads via Turbo SDK (chain-agnostic, permanent storage)
# The commitment keypair needs SOL funding for Arweave uploads (~$0.001/proof)

# Enable/disable Arweave proof uploads (default: true)
ARWEAVE_ENABLED=true

# ============================================
# CUSTOM DOMAIN / SSL CONFIGURATION (dstack-ingress)
# ============================================
# Cloudflare API Token
# Create at https://dash.cloudflare.com/profile/api-tokens
# Template: "Edit zone DNS"
# Permissions: Zone - DNS - Edit
# Zone Resources: Include - Specific zone - mysterygift.fun
CLOUDFLARE_API_TOKEN=

# Email for Let's Encrypt notifications
CERTBOT_EMAIL=hello@mysterygift.fun
